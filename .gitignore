__pycache__/
*.pyc
.venv/
venv/
.env
.streamlit/secrets.toml
.pytest_cache/
*.egg-info/
.DS_Store

# NOTE: data/video_index.json is intentionally NOT gitignored. It's
# generated by scripts/refresh_video_index.py rather than hand-authored,
# but the recommended deployment (README.md "Deployment" section) is a
# scheduled job that commits its refreshed output so the running backend
# always has a checked-in copy to fall back to.
